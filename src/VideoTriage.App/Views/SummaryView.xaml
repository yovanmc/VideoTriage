<UserControl x:Class="VideoTriage.App.Views.SummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:VideoTriage.App.Controls"
             xmlns:conv="clr-namespace:VideoTriage.App.Converters">
    <UserControl.Resources>
        <conv:StringToBrush x:Key="StringToBrush" />
        <conv:StringToBrushFaint x:Key="StringToBrushFaint" />
        <SolidColorBrush x:Key="DangerBrush" Color="#FF6B6B" />
        <SolidColorBrush x:Key="SuccessBrush" Color="#5AD17F" />
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RenderTransform>
            <TranslateTransform Y="0" />
        </Grid.RenderTransform>
        <!-- Gentle fade + rise when the summary first becomes visible. -->
        <Grid.Style>
            <Style TargetType="Grid">
                <Style.Triggers>
                    <Trigger Property="IsVisible" Value="True">
                        <Trigger.EnterActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                     From="0" To="1" Duration="0:0:0.18" />
                                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                                     From="8" To="0" Duration="0:0:0.18">
                                        <DoubleAnimation.EasingFunction>
                                            <CubicEase EasingMode="EaseOut" />
                                        </DoubleAnimation.EasingFunction>
                                    </DoubleAnimation>
                                </Storyboard>
                            </BeginStoryboard>
                        </Trigger.EnterActions>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Grid.Style>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Run complete"
                       FontSize="28"
                       FontWeight="SemiBold" />
            <TextBlock Opacity="0.7">
                <Run Text="Completed " /><Run Text="{Binding CompletedAtText, Mode=OneWay}" />
                <Run Text=" · " /><Run Text="{Binding DurationText, Mode=OneWay}" />
            </TextBlock>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Grid Width="180" Height="180">
                    <controls:DonutChart ItemsSource="{Binding Segments}" />
                    <StackPanel HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                IsHitTestVisible="False">
                        <TextBlock Text="{Binding ProcessedCount}"
                                   FontSize="22"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center" />
                        <TextBlock Text="processed"
                                   FontSize="11"
                                   Opacity="0.6"
                                   HorizontalAlignment="Center" />
                    </StackPanel>
                </Grid>

                <ItemsControl ItemsSource="{Binding Segments}" Margin="16,0,0,0" VerticalAlignment="Center">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="0,3">
                                <Border Width="10" Height="10" CornerRadius="2" VerticalAlignment="Center"
                                        Background="{Binding Color, Converter={StaticResource StringToBrush}}" />
                                <TextBlock Margin="8,0,0,0" VerticalAlignment="Center">
                                    <Run Text="{Binding Label, Mode=OneWay}" /><Run Text="  " /><Run Text="{Binding Count, Mode=OneWay}" FontWeight="SemiBold" />
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <UniformGrid Grid.Column="1" Columns="2" Margin="28,0,0,0" VerticalAlignment="Center">
                <StackPanel Margin="8">
                    <TextBlock Text="Space saved" Opacity="0.65" />
                    <TextBlock Text="{Binding BytesSavedText}" FontSize="24" />
                </StackPanel>
                <StackPanel Margin="8">
                    <TextBlock Text="Replaced" Opacity="0.65" />
                    <TextBlock Text="{Binding ReplacedCount}" FontSize="24" />
                </StackPanel>
                <StackPanel Margin="8">
                    <TextBlock Text="Kept original" Opacity="0.65" />
                    <TextBlock Text="{Binding KeptOriginalCount}" FontSize="24" />
                </StackPanel>
                <StackPanel Margin="8">
                    <TextBlock Text="Overall reduction" Opacity="0.65" />
                    <TextBlock Text="{Binding OverallReductionText}" FontSize="24" />
                </StackPanel>
            </UniformGrid>
        </Grid>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Files}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="64" />
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Border Width="56" Height="32"
                                    Background="#247F7F7F"
                                    ClipToBounds="True"
                                    VerticalAlignment="Center">
                                <!-- Opacity defaults to 1 so a thumbnail is never stuck hidden if
                                     TargetUpdated doesn't fire; From=0 only adds a fade on arrival. -->
                                <Image Source="{Binding Thumbnail, NotifyOnTargetUpdated=True}" Stretch="Uniform">
                                    <Image.Triggers>
                                        <EventTrigger RoutedEvent="Binding.TargetUpdated">
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                     From="0" To="1" Duration="0:0:0.25" />
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </Image.Triggers>
                                </Image>
                            </Border>

                            <TextBlock Grid.Column="1"
                                       Text="{Binding FileName}"
                                       TextTrimming="CharacterEllipsis"
                                       VerticalAlignment="Center"
                                       Margin="10,0"
                                       ToolTip="{Binding FullPath}" />

                            <Border Grid.Column="2"
                                    CornerRadius="4"
                                    Padding="8,2"
                                    Margin="6,0"
                                    VerticalAlignment="Center"
                                    Background="{Binding StatusColor, Converter={StaticResource StringToBrushFaint}}">
                                <TextBlock Text="{Binding StatusLabel}"
                                           FontSize="11"
                                           Foreground="{Binding StatusColor, Converter={StaticResource StringToBrush}}" />
                            </Border>

                            <TextBlock Grid.Column="3"
                                       VerticalAlignment="Center"
                                       Margin="10,0">
                                <Run Text="{Binding OldSizeText, Mode=OneWay}" Foreground="{StaticResource DangerBrush}" /><Run Text=" → " /><Run Text="{Binding NewSizeText, Mode=OneWay}" Foreground="{StaticResource SuccessBrush}" />
                            </TextBlock>

                            <TextBlock Grid.Column="4"
                                       Text="{Binding SavedText}"
                                       Foreground="{StaticResource SuccessBrush}"
                                       VerticalAlignment="Center"
                                       Margin="6,0" />

                            <Button Grid.Column="5"
                                    Content="Reveal"
                                    Margin="6,0,0,0"
                                    VerticalAlignment="Center"
                                    Command="{Binding DataContext.RevealCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                    CommandParameter="{Binding RevealTargetPath}" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
