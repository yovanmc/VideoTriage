<UserControl x:Class="VideoTriage.App.Views.DiagnosticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel>
        <DockPanel>
            <TextBlock Text="Diagnostics" FontWeight="SemiBold" />
            <Button DockPanel.Dock="Right"
                    Content="Clear"
                    Command="{Binding ClearCommand}" />
        </DockPanel>

        <TextBlock Margin="0,8,0,2"
                   Text="{Binding ErrorCount, StringFormat=Messages: {0}}" />
        <TextBox Text="{Binding LogPath, Mode=OneWay}"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 BorderThickness="0"
                 Background="Transparent" />

        <ItemsControl Margin="0,8,0,0" ItemsSource="{Binding Errors}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Expander Margin="0,0,0,6">
                        <Expander.Header>
                            <StackPanel>
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding Message}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Expander.Header>
                        <TextBox Text="{Binding Detail}"
                                 IsReadOnly="True"
                                 TextWrapping="Wrap" />
                    </Expander>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
