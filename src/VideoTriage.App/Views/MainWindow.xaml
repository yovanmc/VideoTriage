<ui:FluentWindow x:Class="VideoTriage.App.Views.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:views="clr-namespace:VideoTriage.App.Views"
                 xmlns:conv="clr-namespace:VideoTriage.App.Converters"
                 Title="VideoTriage"
                 Width="1180"
                 Height="760"
                 MinWidth="900"
                 MinHeight="600"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 WindowStartupLocation="CenterScreen">
    <ui:FluentWindow.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#5CC8FF" />
        <SolidColorBrush x:Key="SuccessBrush" Color="#5AD17F" />
        <SolidColorBrush x:Key="WarningBrush" Color="#E8C35A" />
        <SolidColorBrush x:Key="DangerBrush" Color="#FF6B6B" />

        <conv:RunStateToVisibility x:Key="RunStateToVisibility" />
        <conv:NotNullToVisibility x:Key="NotNullToVisibility" />
        <conv:NullToVisibility x:Key="NullToVisibility" />
        <conv:BoolToVisibility x:Key="BoolToVisibility" />
        <conv:SeverityToBrush x:Key="SeverityToBrush" />
        <conv:RunFailedToVisibility x:Key="RunFailedToVisibility" />
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="44" />
            <RowDefinition Height="*" />
            <RowDefinition Height="58" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="VideoTriage"
                     Icon="pack://application:,,,/Assets/app.ico" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="288" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Padding="16"
                    BorderBrush="#22000000"
                    BorderThickness="0,0,1,0">
                <ScrollViewer VerticalScrollBarVisibility="Hidden">
                <StackPanel>
                    <TextBlock Text="Source folder" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}" />
                    <TextBlock Margin="0,6,0,10"
                               Text="{Binding SelectedFolder}"
                               TextWrapping="Wrap"
                               Opacity="0.7" />
                    <Button Content="Choose folder"
                            Command="{Binding ChooseFolderCommand}" />

                    <TextBlock Margin="0,24,0,4" Text="Preset" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}" />
                    <TextBlock Text="VideoTriage AV1 · read-only scan" Opacity="0.7" TextWrapping="Wrap" />

                    <TextBlock Margin="0,24,0,8"
                               Text="Prerequisites"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource AccentBrush}" />
                    <ItemsControl ItemsSource="{Binding Prerequisites}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,3"
                                      ToolTip="{Binding InstallHint}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Name}"
                                               VerticalAlignment="Center" />
                                    <Ellipse Grid.Column="1"
                                             Width="10"
                                             Height="10"
                                             VerticalAlignment="Center">
                                        <Ellipse.Style>
                                            <Style TargetType="Ellipse">
                                                <Setter Property="Fill" Value="{StaticResource SuccessBrush}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsAvailable}" Value="False">
                                                        <Setter Property="Fill" Value="{StaticResource DangerBrush}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Ellipse.Style>
                                    </Ellipse>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Separator Margin="0,20,0,16" />
                    <views:SettingsView DataContext="{Binding Settings}" />
                </StackPanel>
                </ScrollViewer>
            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="64" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Padding="16,10"
                        BorderBrush="#22000000"
                        BorderThickness="0,0,0,1">
                    <StackPanel Orientation="Horizontal">
                        <ui:Button x:Name="StartButton"
                                   Content="Start"
                                   Appearance="Primary"
                                   Width="92"
                                   Command="{Binding StartCommand}"
                                   ToolTip="{Binding StartBlockedReason}"
                                   Visibility="{Binding RunState, Converter={StaticResource RunStateToVisibility}, ConverterParameter=Idle}" />
                        <Button x:Name="StopButton"
                                Content="Stop"
                                Width="92"
                                Background="{StaticResource DangerBrush}"
                                Command="{Binding StopCommand}"
                                Visibility="{Binding RunState, Converter={StaticResource RunStateToVisibility}, ConverterParameter=Active}" />
                        <Button x:Name="PauseButton"
                                Content="Pause"
                                Width="92"
                                Margin="8,0,0,0"
                                Command="{Binding PauseCommand}"
                                Visibility="{Binding RunState, Converter={StaticResource RunStateToVisibility}, ConverterParameter=Running}" />
                        <Button x:Name="ResumeButton"
                                Content="Resume"
                                Width="92"
                                Margin="8,0,0,0"
                                Command="{Binding ResumeCommand}"
                                Visibility="{Binding RunState, Converter={StaticResource RunStateToVisibility}, ConverterParameter=Paused}" />
                        <Button Content="Back to queue"
                                Margin="8,0,0,0"
                                Command="{Binding BackToQueueCommand}"
                                Visibility="{Binding LastSummary, Converter={StaticResource NotNullToVisibility}}" />
                        <Button Content="Open run data"
                                Margin="8,0,0,0"
                                Command="{Binding OpenDataDirectoryCommand}"
                                Visibility="{Binding LastSummary, Converter={StaticResource NotNullToVisibility}}" />
                        <StackPanel Margin="16,0,0,0"
                                    Orientation="Horizontal"
                                    VerticalAlignment="Center"
                                    Visibility="{Binding IsScanning, Converter={StaticResource BoolToVisibility}}">
                            <ui:ProgressRing IsIndeterminate="True"
                                             Width="16"
                                             Height="16" />
                            <TextBlock Text="Scanning…"
                                       Margin="8,0,0,0"
                                       VerticalAlignment="Center" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0"
                        Background="#33E8C35A" CornerRadius="6" Padding="12,8" Margin="16,16,16,0"
                        Visibility="{Binding InterruptedRunNotice, Converter={StaticResource NotNullToVisibility}}">
                    <DockPanel>
                        <Button DockPanel.Dock="Right" Content="Dismiss" Command="{Binding DismissInterruptedNoticeCommand}" />
                        <Button DockPanel.Dock="Right" Content="Open run data" Margin="0,0,8,0" Command="{Binding OpenDataDirectoryCommand}" />
                        <TextBlock Text="{Binding InterruptedRunNotice}" TextWrapping="Wrap" VerticalAlignment="Center" />
                    </DockPanel>
                </Border>
                <TextBlock Grid.Row="1" Margin="16,12,16,0" Opacity="0.8" Text="{Binding QueueSummaryText}"
                           Visibility="{Binding LastSummary, Converter={StaticResource NullToVisibility}}" />

                <Grid Grid.Row="2">
                <ListBox
                         Margin="16"
                         ItemsSource="{Binding Items}"
                         BorderThickness="0"
                         Background="Transparent">
                    <ListBox.Style>
                        <Style TargetType="ListBox">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding DataContext.LastSummary,
                                    RelativeSource={RelativeSource AncestorType={x:Type ui:FluentWindow}}}"
                                             Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.Style>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,10"
                                    Padding="12"
                                    CornerRadius="8"
                                    Background="#0F7F7F7F">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="96" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="180" />
                                    </Grid.ColumnDefinitions>
                                    <Border Width="96"
                                            Height="56"
                                            CornerRadius="4"
                                            ClipToBounds="True"
                                            Background="#247F7F7F">
                                        <Image Source="{Binding Thumbnail, Mode=OneWay}"
                                               Stretch="UniformToFill" />
                                    </Border>
                                    <StackPanel Grid.Column="1" Margin="14,0">
                                        <TextBlock Text="{Binding FileName}"
                                                   FontWeight="SemiBold"
                                                   TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text="{Binding MetaLine}"
                                                   Opacity="0.7"
                                                   TextTrimming="CharacterEllipsis" />
                                        <ProgressBar Height="4"
                                                     Margin="0,8,0,0"
                                                     Minimum="0"
                                                     Maximum="100"
                                                     IsIndeterminate="{Binding IsProgressIndeterminate, Mode=OneWay}"
                                                     Value="{Binding Progress, Mode=OneWay}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="{Binding StatusText}"
                                                   HorizontalAlignment="Right"
                                                   TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text="{Binding OldSizeText}"
                                                   HorizontalAlignment="Right"
                                                   Foreground="{StaticResource DangerBrush}" />
                                        <TextBlock Text="{Binding SavedText}"
                                                   HorizontalAlignment="Right"
                                                   Foreground="{StaticResource SuccessBrush}" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <views:SummaryView DataContext="{Binding LastSummary}">
                    <views:SummaryView.Style>
                        <Style TargetType="views:SummaryView">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding DataContext.LastSummary,
                                    RelativeSource={RelativeSource AncestorType={x:Type ui:FluentWindow}}}"
                                             Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </views:SummaryView.Style>
                </views:SummaryView>
                <TextBlock HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Opacity="0.45"
                           IsHitTestVisible="False"
                           TextAlignment="Center"
                           Text="No candidates found.&#x0a;Choose a folder with compressible video files.">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <MultiDataTrigger>
                                    <MultiDataTrigger.Conditions>
                                        <Condition Binding="{Binding Items.Count}" Value="0" />
                                        <Condition Binding="{Binding IsScanning}" Value="False" />
                                        <Condition Binding="{Binding LastSummary}" Value="{x:Null}" />
                                    </MultiDataTrigger.Conditions>
                                    <Setter Property="Visibility" Value="Visible" />
                                </MultiDataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                </Grid>
                </Grid>
            </Grid>
        </Grid>

        <Border Grid.Row="2"
                Padding="18,0"
                BorderBrush="#22000000"
                BorderThickness="0,1,0,0"
                Background="{Binding LastSummary.Severity, Converter={StaticResource SeverityToBrush}}">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border BorderBrush="#735CC8FF"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="8,2">
                        <TextBlock Text="{Binding QueueRemainingCount, StringFormat=Queue: {0} files}" />
                    </Border>
                    <TextBlock Margin="12,0,0,0"
                               VerticalAlignment="Center"
                               Opacity="0.85"
                               Text="{Binding RunProgressText}" />
                </StackPanel>
                <StackPanel HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Orientation="Horizontal">
                    <TextBlock VerticalAlignment="Center"
                               Text="{Binding StatusMessage}" />
                    <ui:HyperlinkButton Margin="12,0,0,0"
                                        Content="Open log"
                                        Command="{Binding OpenLogCommand}"
                                        Visibility="{Binding StatusMessage, Converter={StaticResource RunFailedToVisibility}}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>
